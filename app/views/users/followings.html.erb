<h1><%= t('views.users.followings') %></h1>
<ul>
  <% @followings.each do |user| %>
    <li><%= link_to(user.name, user) %></li>
  <% end %>
</ul>
<%= paginate @followings %>
<%= link_to t('views.common.back'), @user %>
